<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <Properties>
        <!-- Variables tomadas del application.properties -->
        <property name="logging.path">${spring:logging.path}</property>
        <property name="max.size">${spring:logging.max.size}</property>
        <property name="application.name">${spring:application.name}</property>
    </Properties>

    <Appenders>

        <!-- Consola -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="(%d %p %C{1.} [%t] %m%n" />
        </Console>

        <!-- Log en archivo -->
        <RollingFile
                name="RollingFile"
                fileName="${logging.path}/${application.name}.log"
                filePattern="${logging.path}/${application.name}-%d{dd-MMMM-yyyy}-%i.log.gz">

            <PatternLayout pattern="%d %p %C{1.} [%t] %m%n" />

            <Policies>
                <OnStartupTriggeringPolicy/>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="${max.size}"/>
            </Policies>

            <DefaultRolloverStrategy max="1"/>
        </RollingFile>

    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console" />
        </Root>

        <Logger name="co.vinni" level="trace">
            <AppenderRef ref="RollingFile" />
        </Logger>
    </Loggers>

</configuration>
